.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Regenerate with `python -m mlbpred.ingest` / `build_dataset` - don't commit data.
data/raw/*.parquet
data/processed/*.parquet
models/*.joblib
models/metadata.json
reports/predictions_*.csv

# reports/backtest_*.csv and market_eval_*.csv ARE committed on purpose:
# they are the evidence behind the numbers quoted in the README.
